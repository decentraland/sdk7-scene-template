See AGENTS.md for project instructions.

This project relies on the Decentraland SDK skills (`sdk-scenes` and its topic skills). Check your available skills list first — they may already be installed at user, project, or plugin scope. Only if they are not listed there, install them with `npx skills add decentraland/sdk-skills --all`.
